def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
      inter  = set()
      res = set()
      for num in nums1:
          inter.add(num)
      for num in nums2:
          if num in inter:
              res.add(num)
      return(list(res))
      
